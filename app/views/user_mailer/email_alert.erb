Collabbit <%=h(@user.instance.long_name)%>

<%=h(@update.incident.name)%> - <%=@action%> update <%="'#{h(@update.title)}'"%>
Posted by <%=h(@update.user.full_name)%> <%="(#{@update.issuing_group.name})" if @update.issuing_type == :group%> at <%=time_mdy(@update.updated_at)%>

<%=h(@update.text)%>

Relevant groups: <%= (@update.relevant_groups.map{|g|j(g.name)}).join(', ')%>
Tags: <%= (@update.tags.map{|t|t.name}).join(', ')%>

View this update online: http://<%= @user.instance.short_name %>.collabbit.org<%=incident_update_path(@update.incident, @update)%>

View all updates by your groups online: http://<%= @user.instance.short_name %>.collabbit.org<%= incident_updates_path(@update.incident) %>?groups_filter=mine
